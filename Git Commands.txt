https://git-scm.com/book/pt-br/v1
https://caiojcarvalho.wordpress.com/2017/07/22/definindo-usuario-e-senha-padrao-no-git/

git init
	Inicia um reposit�rio Git

git status
	Exibe informa��o sobre o ramo atual e status do reposit�rio Git

git add arquivo.txt
	Adiciona todos os arquivos do diret�rio	

git add -A
	Adiciona todos os arquivos do diret�rio

git reset arquivo.txt
	Remove somento este item do git add

git reset
	Remove todos os arquivos e diret�rios do git add

git config user.email "matheusjohannaraujo@gmail.com"
	Configura um email padr�o para o reposit�rio

git config user.name "matheusjohannaraujo"
	Configura um nome de usu�rio padr�o para o reposit�rio

git commit -m "Informa��o do commit"
	Registra altera��es do projeto local

git commit --amend
	Desfaz o ultimo commit

git log
	Exibe os logs dos commits e outras informa��es

git remote add origin https://bitbucket.org/matheusjohannaraujo/testes.git
	Adiciona reposit�rio remoto

git remote -v
	Exibe o reposit�rio remoto do projeto

git push -u origin master
	Envia os arquivos para o reposit�rio remoto

git branch nome-do-ramo
	Cria um novo ramo, o ramo principal � o master

git brach
	Lista os ramos existentes e qual deles esta selecionado

git checkout nome-do-ramo
	Muda para outro ramo

git checkout master
	Volta para o ramo principal

git merge nome-do-ramo
	Mescla o ramo master com o ramo "nome-do-ramo"

git branch -d nome-do-ramo
	Exclui o ramo "nome-do-ramo"

git checkout -- arquivo.txt
	Desfaz altera��es no arquivo, CTRL+Z

git stash
	Esconde altera��es

git stash list
	Lista as altera��es escondidas

git stach apply stash@{0}
	Exibe novamente as altera��es

git stash drop stach@{0}
	Exclui altera��o escondida

git pull origin
	Puxa as altera��es de reposit�rio onlide para o reposit�rio local

git clone https://bitbucket.org/matheusjohannaraujo/testes.git
	Baixo um reposit�rio remoto para o computador local